export class Personne {
    num: number;
    nom: string;
    prenom: string;
  
    constructor(num: number, nom: string, prenom: string) {
      this.num = num;
      this.nom = nom;
      this.prenom = prenom;
    }
  }